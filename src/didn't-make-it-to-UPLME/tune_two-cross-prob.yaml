hydra:
  job:
    name: "two-models_${expt.num_passes}-passes"

lambdas: [null, null, null, null, null] # being tuned / set in optuna objective
# [nll, penalty, consistency, repr_consistency, alignment]

main_data: empstories

error_decay_factor: 1.0

lbl_split: 1
num_passes: 4

do_tune: True
n_trials: 10

do_train: False # training immediately after tuning with updated parameters is not properly configured
do_test: False

overwrite_parent_dir: outputs/2025-07-29/16-36-57_empstories_cross-prob_lambdas-[None, None, None, None, None]_tune-True_two-models_4-passes 
